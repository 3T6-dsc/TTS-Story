{
  "daemon": true,
  "run": [
    {
      "method": "shell.run",
      "params": {
        "message": "if [ -f run.sh ]; then chmod +x run.sh && ./run.sh; else if [ -f run.bat ]; then ./run.bat; fi; fi"
      }
    },
    {
      "method": "local.set",
      "params": {
        "url": "http://localhost:5000"
      }
    }
  ]
}
